---
const message =
	"ciao pupi se state leggendo questo bel messaggino probabilmente state vedendo questo sito non ancora completo e quindi state vedendo qualcosa di segreto quindi...";
---

<div class="secret-strip" aria-hidden="true">
	<div class="secret-strip__track">
		<span>{message}</span>
		<span>{message}</span>
	</div>
</div>
